{
  "name": "aryakdev",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "bun scripts/generate-og.mjs && bunx astro dev",
    "build": "bun scripts/generate-og.mjs && bunx astro build",
    "preview": "bunx astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "gray-matter": "^4.0.3",
    "sharp": "^0.33.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
